import { css } from "styled-components";

export const scrollTopOffset = 50;
export const scrollTopNoOffset = 4;

export const buttonBaseStyles = css`
  width: 100px;
  height: 40px;

  text-transform: capitalize;

  border: 1px solid black;
  border-radius: 20px;

  font-size: 1.5rem;
  font-family: "Roboto";
`;
